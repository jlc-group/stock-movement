-- 003_movement_warehouse.sql — เก็บ "คลังที่รับเข้า" ต่อรายการ (แบรนด์ที่ยึดคลัง: พรีเมี่ยม/Beauterry)
--
-- เดิมระบบเก็บเฉพาะยอดรวมต่อคลัง (ตาราง premium_warehouse: lamlukka/soi8) — ตอนบันทึก
-- รับ-จ่ายรายวันผู้ใช้เลือก in_dest แล้วค่านั้นถูกใช้แค่คำนวณ delta ไม่ได้เก็บติดแถว
-- ทำให้ย้อนดูไม่ได้ว่ารายการรับเข้าแต่ละวันเข้าคลังไหน.
--
-- คอลัมน์ใหม่ wh_in: 'soi8' | 'lamlukka' | 'mixed' | NULL
--   'mixed' = วันเดียวกัน (สินค้า+ช่องทางเดียวกัน) มีรับเข้าหลายครั้งคนละคลัง (สะสมทับกัน)
--   NULL    = รายการเก่าก่อน migration / ไม่มีรับเข้า / สินค้าไม่ยึดคลัง
-- ฝั่งจ่ายออกไม่ต้องมีคอลัมน์ — ระบบตัดจากคลังซอย8 เสมอ (กติกาใน apply_premium_warehouse_delta)
ALTER TABLE stock_online.stock_movements
    ADD COLUMN IF NOT EXISTS wh_in TEXT;
